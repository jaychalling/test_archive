# High Engagement UI Spec

> Visual Density for Maximum Dwell Time
> Test-Archive Frontend Engagement Rules v2.0

## 목적

- 평균 체류시간(Avg Engagement Time) 극대화
- 스크롤 지속률 증가
- "테스트만 하고 나가는 페이지" → "결과를 탐색·해석·공유하는 페이지"로 전환
- AdSense RPM 상승에 기여하는 UX 구조 확보

본 규칙은 3-Step State Machine 구조를 유지하면서,
**의도적으로 시각적 요소 사용을 늘리는 것을 허용·권장**한다.

---

## 1. 핵심 철학

### 1.1 체류시간은 '정보량'이 아니라 '탐색 시간'에서 나온다

- 결과만 보여주는 페이지 → 빠르게 캡처하고 이탈
- 시각적 블록이 많은 페이지 →
  - 멈춤
  - 해석
  - 비교
  - 공유 고민

👉 **체류시간은 "결과 확인"이 아니라 "결과 탐색"의 결과**

---

## 2. UI 사용에 대한 기본 스탠스

| 항목 | 기존 | v2.0 (체류시간 기준) |
|------|------|---------------------|
| UI Block 수 | 최소 충족 | **적극 사용 권장** |
| 박스/카드 | 제한 | **의미만 있으면 허용** |
| 반복 | 금지 중심 | **패턴 반복 허용** |
| 목표 | 결과 전달 | **탐색 + 해석 + 공유 + 체류** |

---

## 3. 확장된 UI Block 세트 (Engagement 중심)

### 3.1 Core Blocks (기본)

- **ScoreBlock**
  - 큰 점수/결과 + 보조 설명
  - 스크롤 멈춤 유도
  - 예: "Your Score: 85" + 결과 해석

- **InsightCard**
  - 결과 해석, 맥락, 배경 설명
  - "이 점수가 의미하는 것"

- **Callout**
  - 한 줄 핵심 요약
  - 인사이트 강조

- **ComparisonTable**
  - 점수대별 비교
  - 다른 사용자 평균과 비교

---

### 3.2 Engagement Blocks (체류 전용)

- **PersonaCard**
  - "당신의 유형은 X입니다" 형식
  - 사용자 정체성 부여

- **ScoreBreakdown**
  - 카테고리별 점수 시각화
  - 강점/약점 분석

- **RankingBlock**
  - "상위 X%에 해당합니다"
  - 사회적 비교 유도

- **Myth vs Fact Block**
  - 테스트 관련 오해/진실
  - 추가 정보 탐색 유도

- **Pros / Cons Grid**
  - 결과의 장단점 분석
  - 판단 지연 → 체류 증가

- **Visual Progress Bar**
  - 읽지 않고 체크하게 만드는 구조
  - 카테고리별 시각적 비교

- **Inline Chart / Bar**
  - 이미지 없이도 시각적 밀도 상승

---

### 3.3 Action Blocks (공유/참여 유도)

- **ShareCard**
  - 공유 버튼 + 공유 시 보이는 미리보기
  - "친구에게 도전하기" CTA

- **RelatedTestCards**
  - 관련 테스트 추천 (2~4개)
  - "이 테스트도 해보세요"

- **RetakePrompt**
  - 재시도 유도 카드
  - "다른 결과를 원한다면?"

---

## 4. Step별 UI 사용 기준

### 4.1 Landing Page (첫인상 + 시작 유도)

**필수 요소**
- Hero Section: 테스트 제목 + 시각적 임팩트
- StatBlock: 참여자 수, 평균 점수 등 (1~2개)
- 시작 버튼: 명확한 CTA

**권장 추가**
- Callout: 테스트 특징 한 줄 요약
- Preview Card: "이런 결과를 알 수 있어요"

---

### 4.2 Quiz Page (집중 + 진행감)

**필수 요소**
- Progress Bar: 시각적 진행률
- 문항 전환 애니메이션: 300ms

**권장 추가**
- 카테고리 표시: 현재 섹션 힌트
- 문항 번호: X / Total 형식

**금지**
- 너무 많은 시각 요소 (집중 방해)
- 광고 배치

---

### 4.3 Result Page (체류시간 핵심 페이지) ★★★

**최소 기준 (v2.0)**

- UI Block **5종 이상 필수**
- 전체 블록 수 제한 없음 (의미만 있으면 허용)

**권장 조합 예**

```
ScoreBlock (메인 점수)
    ↓
PersonaCard (유형/성향 해석)
    ↓
ScoreBreakdown (카테고리별 분석)
    ↓
RankingBlock (상위 X%)
    ↓
InsightCard (상세 해석 2~3개)
    ↓
ComparisonTable (점수대별 비교)
    ↓
ShareCard + 4버튼 그리드
    ↓
RelatedTestCards (추천 테스트)
```

**강제 규칙**
- 스크롤 중간마다 시각적 전환 포인트 존재
- "텍스트-only 구간" 최대 2단락까지만 허용
- 4버튼 그리드 필수 (Share Test, Share Result, Retake, Home)

---

## 5. 반복 사용에 대한 재정의

### 5.1 반복 = 나쁜 것이 아니다

- 동일 UI 패턴 반복 **허용**
- 단, 내용(데이터/해석/카테고리)은 달라야 함

### 5.2 허용되는 반복

- InsightCard 여러 개 (카테고리별)
- ScoreBreakdown 연속 배치
- PersonaCard 다중 등장 (세부 유형별)

### 5.3 금지되는 반복

- 내용만 바뀐 의미 없는 카드
- "박스화된 문단"의 기계적 반복

---

## 6. 체류시간 최적화 체크리스트

배포 전 반드시 확인:

- [ ] Result 페이지에 UI Block 5종 이상
- [ ] 스크롤 중 최소 5회 이상 "멈출 포인트" 존재
- [ ] 숫자/표/비교 블록 최소 3개
- [ ] 사용자가 자기 결과를 해석할 수 있는 섹션 존재
- [ ] 페이지 상·중·하 시각 밀도 차이 존재
- [ ] 4버튼 그리드 구현
- [ ] 관련 테스트 추천 카드 존재

---

## 7. Quality Gate 연동

### G9: High Engagement UI (신규)

| 검증 | 기준 | 실패 시 |
|------|------|---------|
| Result UI Block 수 | 5종 이상 | WARNING |
| 시각적 멈춤 포인트 | 5회 이상 | WARNING |
| 숫자/비교 블록 | 3개 이상 | WARNING |

---

## 8. KPI 연결

이 규칙은 다음 지표를 직접 겨냥한다:

- Avg engagement time ↑
- Scroll depth ↑
- Share rate ↑
- Retake rate ↑
- RPM ↑ (광고 노출 기회 증가)

> 체류시간은 테스트 품질의 결과가 아니라
> **결과 페이지 구조와 시각 리듬의 결과다.**

---

## 최종 원칙 요약

- UI 요소를 "아껴 쓰는 시대"는 끝났다
- Test-Archive에서는:
  - **Result = 점수만 보여주는 페이지 ❌**
  - **Result = 탐색하고 해석하는 리포트 ⭕**
- 더 많은 UI 요소 사용은 리스크가 아니라 **전략 자산**

---

## Framer Motion 통합

모든 Engagement Block에 애니메이션 적용 권장:

```typescript
// 블록 순차 등장
const containerVariants = {
  hidden: { opacity: 0 },
  visible: {
    opacity: 1,
    transition: {
      staggerChildren: 0.1, // 순차 등장
    },
  },
};

const itemVariants = {
  hidden: { opacity: 0, y: 20 },
  visible: { opacity: 1, y: 0 },
};

// 점수 카운트업
const ScoreBlock = ({ score }: { score: number }) => {
  const [displayScore, setDisplayScore] = useState(0);

  useEffect(() => {
    // 0에서 score까지 카운트업 애니메이션
    const duration = 1500;
    const start = Date.now();
    const animate = () => {
      const elapsed = Date.now() - start;
      const progress = Math.min(elapsed / duration, 1);
      setDisplayScore(Math.floor(progress * score));
      if (progress < 1) requestAnimationFrame(animate);
    };
    animate();
  }, [score]);

  return <div className="text-6xl font-bold">{displayScore}</div>;
};
```

---

## 스펙 참조

- [test-revamp-specialist.md](../subagents/test-revamp-specialist.md)
- [quality-gate-checker.md](../subagents/quality-gate-checker.md)
- [performance-spec.md](./performance-spec.md)
